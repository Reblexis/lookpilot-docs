## LookPilot Configuration
1. Set protocol to `freetrack`
2. Click **Start** to begin tracking

## F1 2018 Setup
Once LookPilot is running, launch the game and head tracking should work automatically. If not, check the game's settings for camera or head tracking options (typically in Settings → Camera Settings or similar).

